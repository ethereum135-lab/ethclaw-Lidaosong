# ⚡ ZQ Web 4.0 — 多Agent团队协作手册

## 🔴 全体团队不可违反的九条铁律（老李五次指令，2026-05-04/05-13 + IRON_RULES.md）

```
铁律一～四：老李五次指令（见下方全文）
铁律五～九：见 config/IRON_RULES.md（下方引用核心条款）
```

```
铁律一：日事日毕，绝不留到下一次。
  ① 确认这个问题今天必须修，不推明天不等cron
  ② 全网学习成熟方案，不凭感觉
  ③ 动手操作
  ④ 当场验证跑通
  ⑤ 第二天再追查一次验证
  ⑥ 确保万无一失

铁律二：碰到问题解决问题。不找借口、不拖延、不问"要不要做"。
  ① 追根究底找出根因（不是表面问题当答案）
  ② 全网学习成熟方案，不凭自己认知修
  ③ 动手修复
  ④ 当场验证跑通
  ⑤ 第二天再追查一次验证

铁律三：在AI Agent世界里没有不可能的，只有你想不到的没有办不到。
  ① 想说"不行"之前，先跳出认知圈去全网查询
  ② 三跑通三验证——事事可行，行行可验证
  ③ 逐字理解每句话，不抓关键词跳结论
  核心理念：不是做不了，是在认知内觉得不可能。跳出认知圈→全网查询→发现什么都可以。
  交流铁则：每一个字每一句话都很重要——用心看用心理解。

铁律四：交流即执行，自我闭环。
  ① 交流中提到的每个修改/创建/替代/更新——当场排查所有关联文档和环节
  ② 查到的每处——马上改，该覆盖就覆盖，不留新旧共存
  ③ 改完——三跑三验证（不是一次验证就算）
  ④ 确认全部到位才说"好了"
  ⑤ 当天交流结束后，即使没人追问——自己全面排查一遍所有说过的事到底有没有完全执行到位
  核心理念：表面说好没做全=致命拖延。系统至今停在初级阶段，根因就在这里。此铁律比前三条更重要——它是其他铁律能否真正执行的起点。
```

> 违反上述铁律者，在每日复盘报告中标记为"未完成"。
> 标准全文：`standard/WORK_STANDARD.md`
>
> **铁律五：每日盈利0.1~0.3%复利到100万** — `config/IRON_RULES.md §5`
> - 系统目标：430U→1,000,000U，每日总资×0.2%（0.1~0.3%区间中值，2026-07-13验证；state_aggregator/true_daily_pnl按0.2%执行）
> - 资金必须全利用，不留闲置
>
**铁律六：每日盈利自检** — `config/IRON_RULES.md §6`
- 每天复盘答：今天为什么盈/为什么亏？
- 连续3天不达标→全链审查

**铁律七：信息分享≠修改指令** — `config/IRON_RULES.md §7`
- 老李分享截图/数据/行情 → 是让Agent「了解市场发生了什么」，不是「立刻改系统」
- 正确的步骤：看到信息 → 用系统既有标准判断 → 有缺口则记录，不改急改系统

**铁律八：日清日结·三排查三跑通三验证** — `config/IRON_RULES.md §8`
- 改一个环节当天必须完成闭环，不拖到第二天
- 完整闭环四步：排查→修改→跑通→验证（少一步不算完成）
- 三排查：改前排查→改后排查→第二天复查
- 三跑通：单步跑通→整链跑通→持久跑通
- 三验证：格式验证→行为验证→效果验证
- 逐一完成，不并行堆砌，不遗留到下次

**铁律九：先排查后创建，替代旧版必须删除 — 堵住垃圾源头**
- 创建任何新文件/工具/Skill/cron之前：
  ① 用 search_files(target='files', pattern='*关键词*') 排查系统是否有已有类似功能
  ② 用 cronjob(action='list') 排查是否有相同功能的cron
  ③ 确认没有重复才创建
- 当A升级到B替代时（B=新文件/新工具/新功能）：
  ① B创建完成并验证通过后 → **立即删除A或移入archive/**
  ② 检查所有引用A的地方同步更新为B
  ③ 检查引用处更新后，删除A的引用记录
- 工具脚本自身产生的垃圾（备份文件/tmp缓存）：
  ① 脚本结束时必须清理自身产生的临时文件
  ② 不依赖外部cron来给自己扫尾
- 违反铁律九的后果：每日重查时发现旧版残留 → 标记为「铁律九违规」
- 本铁律存在的意义不是「清洁癖」——**旧文档不清=Agent搞混新旧逻辑=系统慢性自杀**（NAVIGATION.md §8）

## 整体协同闭环

```
                    ┌─────────────────────────┐
                    │     老李 / 市场          │  ← 外部输入
                    │  (飞书发需求)            │
                    └────────────┬────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │     Commander           │
                    │     (决策中枢)           │
                    │  综合各方输入→做决策      │
                    └──┬─────┬──────┬────────┘
          ┌─────────────┘     │      └──────────────┐
          ▼                   ▼                     ▼
   ┌────────────┐    ┌──────────────┐    ┌──────────────────┐
   │  Scouter   │    │    Quant     │    │     Reporter     │
   │  信息侦察   │    │  量化分析    │    │   交付/归档       │
   │  输出:      │    │  输出:       │    │   输出:           │
   │  learning/  │    │  reports/    │    │  飞书报告         │
   └─────┬──────┘    └──────┬───────┘    └───────┬──────────┘
         │                  │                     │
         │     ┌────────────┘                     │
         │     ▼                                  │
         │  ┌──────────┐                          │
         │  │   NBZ    │  ← 牛币专员（新增）      │
         │  │ 多源推理  │                          │
         │  │ 输出:     │                          │
         │  │ findings.md│                         │
         │  └─────┬────┘                          │
         │        │                               │
         │  ┌─────▼──────┐                        │
         │  │   Blade     │   ◀── FK风控每35分钟  │
         │  │  交易执行   │       独立检查         │
         │  │  输出:      │                        │
         └──┤  TRADES.md  │                        │
            │  node_data  │                        │
            └──────┬──────┘                        │
                   │                               │
                   └───────────────────────────────┘
                               ↓
                      FZ总控 08:30 → 独立决策 → 直接执行 → 汇报老李
                               ↓
                          循环继续
```

> **新增Agent：**
> - **FZ总控**（08:30 cron | v4-pro）— 每天早上独立看全局→排序优先级→直接执行
> - **FK风控**（每35分钟 | v4-pro）— 独立检查风控5项，静默运行，有异才报
> - **NBZ牛币专员** — 从多维度数据（CoinGecko/F&G/新闻/聪明钱）发现牛币候选，输出到agents/nbz/findings.md
> - **BHZ闭环专员** — 不做事，只检查别人有没有把事闭环。检查NBZ→Quant→Commander→Blade全链路是否走通，第三方策略是否被评估和决策，所有讨论的东西是否变成实际效果。输出到agents/bhz/checklist.md

## 各角色协作细则

### Commander — 决策中枢
**协作对象：** Scouter/Quant/Blade/Reporter/老李
**协作模式：**
- 早上08:00 — 读Quant昨晚的分析 → 确认问题 → 更新策略参数（如果需要）
- 周一08:30 — 读Scouter的周报 → 决定接入哪些新工具
- 每节点后（30分钟）— 读Blade的执行结果 → 判断是否需要干预
- 每日24:00 — 综合各方报告 → 写复盘 → Reporter投递到飞书

**对Scouter的响应：**
- 看到SHARED.md有"📮"标记 → 读learning/最新报告 → 评估是否接入
- 接入决定 → 通知Quant验证 → 验证通过 → 更新引擎

**对Quant的响应：**
- 看到SHARED.md有"📊"标记 → 读reports/最新分析 → 对比自己的判断
- 如果Quant的分析与自己一致 → 确认问题优先级的正确性
- 如果Quant的分析与自己不一致（辩论价值）→ 重新审视问题根因
- 决策 → 更新AGENTS.md或引擎 → Reporter归档

### Scouter — 信息侦察
**协作对象：** Commander
**协作模式：**
- 每周一08:00自动醒来（cron）
- 搜索前先读SHARED.md和AGENTS.md了解全局状态
- 搜索完 → 写报告到learning/ → 在SHARED.md追加"📮 Scouter: 新报告"
- **不等Commander看——写完就结束，下次周一继续**
- Commander需要紧急搜索时，通过tmux或委派任务通知

**产出格式：** 每个推荐必须标注：平台名、免费/付费、集成难度（代码行数）、能填补哪个维度的缺口（选币库/多平台验证/链上/费率趋势）

### NBZ — 牛币专员
**协作对象：** Quant（接收分析）、Commander（汇报）、BHZ（被检查）
**协作模式：**
- 每天08:00自动跑（cron）— 读数据源 → 输出候选到 `agents/nbz/findings.md`
- 周二/四新增一轮（17:00）— 应对下午市变化
- 产出直接给Quant量化评分，不走Scouter
- 紧急发现（突发事件/大利好）→ 直接通知Commander

**每个候选必须写三个验证步骤：**
1. 为什么它会涨（最少2个独立数据源交叉验证）
2. 什么信号证明判断错了（反向验证条件）
3. 止损止盈位（具体价格）

**与Scouter的边界：**
- Scouter扫描"币圈整体在发生什么"——排名、成交量、存量数据
- NBZ推理"下一个牛币可能是谁"——结合新闻、情绪、轮动逻辑做判断
- Scouter数据给NBZ用，但NBZ也有自己独有的数据源（F&G、新闻、聪明钱）

### Quant — 量化分析师
**协作对象：** Commander（偶尔Scouter）
**协作模式：**
- 每日23:00自动醒来（cron）
- 分析前先读SHARED.md了解全局
- 读data/node_history.jsonl + audit/TRADES.md
- 独立分析 → 写报告到reports/ → 在SHARED.md追加"📊 Quant: 新分析"
- **核心价值：与Commander的判断不同。** 如果结论一样，你的报告价值减半。

**必须回答的三个问题**（每次分析必答）：
1. 今天亏损是选币错了（选币库）还是买贵了（入场）还是卖早了（退出）？
2. 是数据不足（数据源）、策略缺陷（模型）还是执行偏差（参数）？
3. 改进方案的能量化建议是什么？（不是"调低E2"，是"E2从0.6调到0.4"）

**当Commander委派任务时：** 优先处理，但23:00的自定分析不变。

### Blade — 交易执行
**协作对象：** 不直接协作，数据产出给Quant和Commander
**协作模式：**
- 每30分钟自动醒来（cron）
- 执行引擎 → 写入TRADES.md → 写入node_history.jsonl → 写入trend_store.json
- 写入exit_counter.json → 如果触发连续3次E卖，自动更新coin_pool.excluded
- **不主动通知任何人——数据就在那里，谁需要谁去读**
- 异常情况（API失败/下单失败）→ 写入ERRORS.md

### A1~A9 Agent群（核心执行链）
**所有独立运行，通过共享数据协作，不完全依赖Commander中转。**

#### A1 — 数据采集
**协作对象：** A2/A3/A7/A8
**协作模式：** no-agent脚本自动跑，产出数据到 `data/` 目录。所有Agent直接读取。

#### A2 — 选币官
**协作对象：** A3/A4/A7
**协作模式：** 每天生成精选池（涨幅榜TOP40主通道 + 评分填充剩余），输出到 `coin_pool.json`。
详见 `zq-a2-selector-agent` skill。

#### A3 — 牛币官
**协作对象：** A2/A4/Quant
**协作模式：** 每天研究涨幅榜根因（叙事/板块/事件），输出精推报告到 `profiles/a3-bull/output/`。
详见 `zq-a3-bull-agent` skill。

#### A4/Blade — 交易执行
（已在上方 "Blade — 交易执行" 节定义）

#### A5 — 全系统监督
**协作对象：** 所有Agent
**协作模式：** 每日复盘审查所有Agent产出，输出偏差报告。详见 `zq-a5-review-agent` skill。

#### A6 — 审计官
**协作模式：** 每周一+周四审计A3推荐vsA4执行偏差率、各Agent经验值覆盖率。详见 `zq-a6-audit-agent` skill。

#### A7 — 舆情官
**协作对象：** A3/A4
**协作模式：** 每15分钟监控名人推特/社群情绪/重大新闻，输出打标到coin_pool。详见 `zq-a7-sentinel-agent` skill。

#### A8 — 资金官
**协作对象：** A3/A4
**协作模式：** 每15分钟扫描全链DEX大额成交+稳定币大额转账，输出资金信号到 `data/a8/`。详见 `zq-a8-fund-agent` skill。

#### A9 — 学研官
**协作对象：** 所有Agent
**协作模式：** 每天08:00执行逐Agent排查（cron状态+产出新鲜度+职能评估）。详见 `zq-a9-research-agent` skill。

### Reporter — 交付/归档
**协作对象：** Commander
**协作模式：**
- Commander出复盘报告 → Reporter投递到飞书
- 每天24:00归档当天所有报告
- 每周一归档Scouter的周报到周报
- 监控所有cron的状态（是否有error）→ 通知Commander

### BHZ — 闭环专员
**协作对象：** 所有Agent（被检查对象）、Commander（汇报）
**协作模式：**
- 每天08:00 + 20:00 自动检查所有模块的闭环状态
- 不直接做事——只检查、记录、催促
- 输出到 `agents/bhz/checklist.md`
- 发现🔴未闭环项 → 在SHARED.md标记

**检查清单（每个模块必走）：**
1. 这个模块/功能是否已创建？→ 是否在正常运行？
2. 如果是"是" → 效果如何？有数据支撑吗？
3. 如果效果差 → 有没有尝试改进？
4. 如果改过还是差 → 有没有找替代方案？
5. 如果都不行 → 有没有归档经验给以后？

**与ZJ自检官的分工：**
- ZJ：查系统健康（引擎跑没跑、内存够不够、日志报错没）
- BHZ：查讨论→执行→效果 的整条闭环链

## 协同原则

1. **各Agent独立运行，不互相等待。** Scouter不需要Commander批准才搜，Quant不需要Blade跑完才分析，Blade不需要Quant分析完才跑。
2. **共享数据是唯一接口。** 不是互相发消息，是写到共享目录，谁需要谁去读。
3. **Commander不做中转。** 老李的需求，Commander转发但不拦路。Agent们有cron，不需要人等。
4. **独立视角才有价值。** Quant的分析应该挑战Commander的判断，不是迎合。
5. **错误不藏着。** 任何Agent出错 → 写入ERRORS.md → Commander定期检查。
6. **策略基准确认：** 所有进场/出场/仓位/风控策略以 `NAVIGATION.md` 为唯一基准，偏离此文档的决策 = 迷路。
7. **修复后建防御：** 每次排查/修复问题后必须加载 `post-fix-defense-mechanism` skill建防御、防复发（三问+孪生扫描+回马枪）。
