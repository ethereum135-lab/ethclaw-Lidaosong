# ZQ Web 4.0 策略框架：根本问题分析

> 分析时间: 2026-07-08
> 分析基础: 项目文件阅读（NAVIGATION.md, ERRORS.md, cron_inventory_report.md, NODES.md, fast_scan.py, executor.py, global_state.md, TRADES.md, narrative_report.md）
> 数据验证: 期初430U → 当前~$66.56（真实数据）

---

## 一、过滤链拆解：14层条件，层层缩紧

### 完整管道（从信号到执行）

```
信号端
  ├── ① 成交量粗筛 ≥ $500K（fast_scan.py 第25行）
  ├── ② 价格波动范围 -30% ~ +20%（fast_scan.py 第337行）
  ├── ③ 成交量TOP 50（fast_scan.py 第341行）
  ├── ④ 1h K线分析（fast_scan.py 第128行）
  │      └── vol_surge ≥ 1.5x / range_pos 25-80% / n_candles_up ≥ 2
  ├── ⑤ Smart Signal 检查（fast_scan.py score_candidate）
  ├── ⑥ A3 STRONG 等级 ≥ 200pts（NAVIGATION.md bypass规则）
  ├── ⑦ Smart Signal = BULLISH/BUY（bypass规则）
  ├── ⑧ pump-guard 24h涨幅 ≤ 30%（bypass规则）
决策端
  ├── ⑨ ZH方向优先级匹配（权重≥20%）（bypass规则）
  ├── ⑩ A3报告标记"高置信度"（bypass规则）
  ├── ⑪ FNG风控仓位上限（4个仓位，FNG≤15%标准仓位）
  ├── ⑫ 5m动量检查（realtime_momentum_check.py — 15分钟必须上涨）
  ├── ⑬ dust保护（<$25自动卖）
  └── ⑭ min_notional（单仓≥$5，微账户<$50时每仓≥$12）
```

**共14层过滤，层层缩紧。** 每一层都可能在极端市场条件下阻断交易。

### 管道错位的致命问题

每个Agent独立输出，但它们的候选币交集必须全部非空：

```
fast_scan高池 ∩ A3 STRONG池 ∩ Smart Signal池 ∩ ZH方向池
```

文档描述的管道错位（bypass注释）说：**45个连续周期中fast_scan高池与A3 STRONG池交集为空**。这意味着45个周期里，不存在任何一个币同时通过第④层和第⑥层。系统增加bypass规则作为"修补"——但这本质上是用一个复杂条件集（6个子条件）来绕过另一个复杂条件集，并没有减少过滤层数。

---

## 二、几层过滤是合理的？几层是过度的？

### 合理层（信号层的本质检查）

| 层 | 类别 | 合理性 | 原因 |
|:--|:-----|:-------|:-----|
| ① 成交量筛 | 排除噪音 | ✅ | 防止无交易量的幽灵币 |
| ② 波动范围 | 排除极端 | ✅ | 防止已泵顶和归零币 |
| ⑧ pump-guard | 防追高 | ✅ | 单一、明确的条件 |
| ⑪ FNG仓位 | 风控 | ✅ | 确定性逻辑，不阻塞进场 |
| ⑬⑭ dust/min | 交易门槛 | ✅ | 交易所硬规则，不可省略 |

### 过度层（导致死锁的核心原因）

| 层 | 类别 | 问题 | 原因 |
|:--|:-----|:----|:-----|
| ③ TOP 50 | 🟡 过度 | 容量限制 | 限制扫描范围过窄，错过尾部机会 |
| ④ 1h K线分析 | 🔴 过度 | 复杂判据 | 4个子条件（vol_surge/range_pos/n_up/chg_1h）全部满足才出一个信号 |
| ⑤ Smart Signal | 🔴 过度 | 外部依赖 | 依赖A8链上数据，数据源本身不稳定（SOCKS5间歇失效） |
| ⑥ A3 STRONG | 🔴 过度 | LLM输出不可靠 | LLM产生的评分（STRONG/SIGNAL/WATCH）有漂移，且依赖A2→A3管道 |
| ⑦ Smart Signal详细 | 🔴 过度 | 条件重复 | BULLISH/BUY是第⑤层的细化 |
| ⑨ ZH方向 | 🔴 过度 | LLM输出 | ZH方向也是LLM生成的，当天方向可能覆盖所有候选币 |
| ⑩ A3高置信度 | 🔴 过度 | 条件重复 | bypass规则中的冗余条件 |
| ⑫ 5m动量 | 🔴 过度 | 时序误杀 | 15分钟未上涨就否决——但买入决策是基于1h K线的 |

### 核心发现

**过度层集中在信号验证端：第④到第⑩层 = 7层（LLM评分 + 多Agent交叉验证 + 实时验证）。**
合理层只需要4层（成交量/波动/泵保护/风控）。

**过度层数量是合理层的2倍。** 每加一层就加一个"管道错位"的可能性。

---

## 三、每次亏损后加新条件 vs 简化系统——哪个方向对？

### 系统条件增长历史（从项目推断）

```
初始: 信号系统（评分自动执行）
  ↓ 亏损后
加 E系列退出信号（E2/E3/E4）
  ↓ 继续亏损后
加 多Agent评分（A2/A3/A4/A5/A7/A8）
  ↓ 继续亏损后
加 fast_scan（独立数据源）
  ↓ 继续亏损后
加 FNG风控
  ↓ 继续亏损后
加 pump-guard
  ↓ 继续亏损后
加 ZH方向优先级
  ↓ 继续亏损后
加 Smart Signal交叉验证
  ↓ 继续亏损后
加 bypass规则（因为管道错位）
  ↓ 继续亏损后
加 5m动量检查
  ↓ 继续亏损后
加 dust/min_notional保护
  ↓ 继续亏损后
加 A5/A6/A9监督/审计/研究
  ↓ 现在（$430→$66.56）
62个cron任务，9个Agent，14层过滤
```

### 分析：加条件的方向是否合理？

**答案是：方向是错误的。**

每一个新条件的加入逻辑是：
> "刚才赚/亏了是因为X条件没检查到→加X条件来防止下次"

这看起来合理，但有三个致命问题：

1. **条件之间存在相互作用（死锁）**：A3候选 + Smart Signal + ZH方向 + 5m动量 + pump-guard。5个条件每个单独看都合理，但结合起来，一个币要通过5个条件的概率是指数级下降的。如果每个条件通过率是30%，5个条件同时通过的概率 = 0.3^5 ≈ 0.24%。这就是48天零交易的根本原因。

2. **每次"解决"一个症状，不是根因**：亏损的根因可能是选币逻辑本身有问题（比如评分系统对"上涨动能"的定义不适合小资金），但每次加条件是在处理"那次为什么亏"的具体场景，而不是"选币逻辑为什么错了"。

3. **条件越多，系统越保守，死亡螺旋越快**：条件多了→没有候选→系统零交易→资金被大盘拖累被动亏损→更焦虑→再加条件→更没有候选。这是一个正反馈的保守螺旋。

### 简化系统的方向是唯一对的

| 方案 | 预期结果 |
|:-----|:---------|
| 加条件 | 过滤更多，交易更少，错过更多机会 |
| 简化 | 降低死锁概率，让资金流动起来 |

**但需要注意：** 简化不是随机砍条件。需要知道哪些条件是保护性的（保留），哪些是阻塞性的（砍掉）。

---

## 四、文档矛盾怎么影响执行？

### 矛盾1：日目标

| 文档 | 说 | 影响 |
|:-----|:--|:-----|
| NAVIGATION.md | 0.1-0.3%/天（行业合理区间） | ❌ |
| global_state.md | +2.1465%/天（$430→$1M/1年） | ✅ |

**影响：** global_state.md是A4执行时实际读取的文件（A4读direction_priority和global_state）。当A4读到"每天必须2.16%"，面对$220的资金，$220×2.16% = $4.75/天。这个数字与行业median（$0.07/天）相差68倍。这种目标不匹配导致A4在心理上倾向于"等大机会来一把"而不是"每天小赚"——直接与NAVIGATION.md的"资金必须工作"矛盾。

### 矛盾2：简单策略 vs 实际执行

| 文档 | 说 | 实际 |
|:-----|:--|:----|
| NAVIGATION.md §九 | tools/simple_strategy.py 每小时在AWS运行 | 文件存在但crontab中没有该条目 |
| NAVIGATION.md §九 | A2-A9不再作为执行前提 | 过滤链仍然依赖A3 STRONG + Smart Signal + ZH方向 |

**影响：** 文档声称的简化策略从未实际执行过。系统运行时仍然依赖完整的9-Agent链。这是**文档漂移**——写了一个漂亮的简化蓝图，但实际系统还在运行旧的多Agent体系。

### 矛盾3：A4独立 vs 管道依赖

| 文档 | 说 | 实际 |
|:-----|:--|:----|
| NAVIGATION.md | "A4不再等待A3/A7/A1的数据才执行。A4独立看市场" | executor.py仍然读A3报告、读signals.json、等fast_scan |
| AGENTS.md | "不选币——那是A3的事" | bypass规则要求A3 STRONG ≥200pts |

**影响：** A4声称"独立"，但实际执行仍依赖A3的输出。这个矛盾让系统处于一种半独立状态：A4想独立但代码需要A3数据，导致旁路规则越来越复杂（bypass的6个条件本身就是一种复杂的依赖管理）。

### 矛盾4：资金必须工作 vs 48天零交易

| 文档 | 说 | 实际 |
|:-----|:--|:----|
| NAVIGATION.md | "资金必须工作，一天都不放过" | 48天零交易 |
| NAVIGATION.md | "USDT闲置>20%时必须开仓" | USDT闲置>50%时仍不开仓 |

**影响：** 文档写了一套活跃交易的原则，但实际系统运行的是保守死锁的系统。文档和实际行为的差距——**不是文档错了，是系统执行不了文档。**

---

## 五、单纯减少条件的假设推演

如果不做任何系统性修改，只砍掉一半以上过滤条件，会发生什么？

### 砍掉的层（假设）

| 砍掉 | 原因 |
|:-----|:-----|
| ③ TOP 50限制 → 扩大到TOP 200 | 增加候选池 |
| ④ 1h K线分析条件放宽 | 减少vol_surge/n_up等复杂判据 |
| ⑤ Smart Signal依赖 | 非必须，作为参考不阻塞 |
| ⑥ A3 STRONG要求 | bypass已证明管道错位 |
| ⑦ Smart Signal详细检查 | 同上 |
| ⑨ ZH方向匹配 | 参考不阻塞 |
| ⑩ A3高置信度 | 冗余 |
| ⑫ 5m动量检查 | 时序误杀严重 |

保留：①成交量、②波动范围、⑧pump-guard、⑪FNG仓位、⑬dust、⑭min_notional

### 推演结果

**短期（头几天）：**
- 候选池从"几乎为空"变为"若干候选"
- 系统开始产生交易信号
- 可能有数笔小额交易
- PnL可能是负的（因为选币逻辑本身没改进）

**中期（1-2周）：**
- 交易频率增加（从48天0笔变为每日数笔）
- 但每笔的盈利/亏损取决于选币逻辑本身的质量
- 如果选币逻辑（评分系统）有系统偏见，交易多≠盈利多
- 可能因为交易频率增加而产生更多摩擦成本（滑点）

**长期（1个月+）：**
- 如果选币逻辑本身是正期望值（哪怕很小）→ 开始盈利
- 如果选币逻辑是零期望或负期望 → 亏损加速（因为没有条件保护了）
- 最大的风险：**过滤条件少意味着更多错误交易**

### 关键不确定性

砍条件解决的是**阻塞问题**，不是**选币质量问题**。

系统当前的死锁不是因为14层过滤把坏币挡住了——而是把**所有币**都挡住了。砍条件能让门打开，但里面装的是什么货（选币信号的质量）决定了最终结果。

用比喻说：当前系统是一个安检过于严格导致乘客全在排队进不了登机口的机场。砍条件就是撤掉多余的安检通道，让乘客能登机。但飞机能不能安全到达目的地，取决于驾驶员的飞行技术和飞机的质量——也就是选币逻辑本身的可靠性。

---

## 六、结论：策略框架的核心问题

### 根本问题：三层故障叠加

1. **架构层：过度工程化**
   - 9个Agent为$200-$400的资金服务——这个资金量级不需要多Agent评分体系
   - 14层过滤中至少8层是过度/阻塞性的
   - 每次加条件时没有考虑"这个新条件会和现有条件产生什么相互作用"

2. **策略层：症状驱动而非根因驱动**
   - 每次亏损后加新条件，形成"条件堆积病"
   - 没有系统性地量化每层的通过率和贡献度
   - 不知道哪些条件是保护资本的有效条件，哪些只是心理安慰

3. **执行层：文档与代码脱节**
   - 简化策略写了但没实际部署（NAVIGATION.md写simple_strategy.py但crontab没有）
   - 全球状态变量保留着2.16%/天的目标，与修正后的0.1-0.3%矛盾
   - A4声称独立但代码实际依赖A3

### 最关键的单一数据点

**$430 → $66.56（-84.5%）**。这个数字说明：
- 系统不但没有盈利，而且以惊人的速度持续亏损
- 48天零交易期间，资金仍然缩水（因为没有交易的账户被大盘波动拖累？还是市场整体下跌？）
- 不论加了多少条件，系统整体是**负期望值**的

### 建议的思维转变

| 当前思维 | 应该的思维 |
|:---------|:-----------|
| 加更多条件来防止亏损 | 去掉多余条件让资金流动 |
| 多Agent评分提高准确性 | 一个简单的RSI+EMA策略可能比9个Agent更可靠 |
| 每天2.16% | 每天0.1%（≈行业median）已经是巨大进步 |
| 文档写得很完美，只是执行不了 | 文档和实际差距越大，问题越严重 |

**总结成一句话：**
> 系统当前的问题不是"条件不够多没拦住坏币"，而是"条件太多把所有币都拦住了，同时选币逻辑本身是负期望值的"。
