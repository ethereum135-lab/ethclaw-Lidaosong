# 盈利铁律 — 复盘体系的唯一底线

## 铁律一：每一天必须盈利

> 430U → 1M，每日总资×0.1~0.3%复利（中值0.2%；2%/日=年化730%数学不可行，2026-07-13全网验证）。

所有复盘、所有工具、所有cron**只为一个目标服务**：今天收盘总资 > 昨天收盘总资。

如果做的一切不能证明「明天能多赚多少」，就是在浪费资源。

## 铁律二：复盘发现的问题必须自动修复

复盘流程最后一步必须是：

```
发现问题 → 自动修复（改脚本/改cron/改配置/改策略）
   ↓
验证效果（跑真实数据对比）
   ↓
输出验证结果（修复前亏$X → 修复后多赚$Y 或 预期改善方向）
```

**不能修复的问题** → 在报告中明确说「这个问题需要你确认，原因是XXX」，不能默默放过。

## 铁律三：修复效果用赚钱验证，不用代码跑通验证

| ❌ 旧标准 | ✅ 新标准 |
|:---------|:----------|
| "代码跑通了，cron状态ok" | "修完后，今天比昨天多赚了$X" |
| "数据管道修复完成" | "修复后，信号比修复前多捕获了N个涨幅榜币，其中M个已转化为盈利" |
| "SSH隧道已重建" | "SSH修复后，A4成功执行了昨天不能执行的N笔交易，净盈利$X" |

说「修好了」必须拿出数字证明。拿不出来就是没修好。

## 铁律四：每个复盘报告必须包含「盈利三问」

无论什么复盘（系统健康/涨幅榜/量化分析），报告末尾必须回答：

```
💰 盈利三问
① 今天赚了吗？→ $X（绝对值，不算百分比）
② 为什么赚/亏？→ 归因到具体一个环节（选币/信号/执行/风控），不超过一句
③ 明天怎么赚更多？→ 1个具体动作（不是分析），当场执行
```

## 铁律五：收盘前必须做盈利冲刺

每天收盘前30分钟执行一次**盈利冲刺检查**：

```bash
# 读当前持仓和浮盈状态
cd ~/zq_web4_trading_system && python3 -c "
import json
try:
    gs = json.load(open('data/global_state.json'))
    bal = gs.get('balance', {})
    equity = bal.get('equity', 0) or bal.get('total_balance', 0)
    yesterday_close = bal.get('yesterday_close', 0)
    pnl_today = equity - yesterday_close
    pnl_target = yesterday_close * 0.002   # 0.1~0.3%区间中值
    print(f'今日总资: \${equity:.2f}')
    print(f'昨日收盘: \${yesterday_close:.2f}')
    print(f'今日盈亏: \${pnl_today:+.2f}')
    print(f'日目标0.2%: \${pnl_target:.2f} (0.1~0.3%)')
    print(f'达成率: {pnl_today/pnl_target*100:.0f}%' if pnl_target else 'N/A')
except Exception as e:
    print(f'读取失败: {e}')
"
```

如果距目标差太多 → 找出可调仓机会 → 直接指示A4执行。

## 铁律六：每个复盘cron必须验证自身效果

每个复盘cron在报告送达用户之前，先自检三个问题：

1. 我这次复盘花了多少token？值得吗？
2. 如果连续3天产出"一切正常" → 说明复盘方法不够深，明天自动升级模式
3. 今天有没有发现新的问题类型？→ 追加到标准检查清单

## 铁律七：打分不再有意义，赚钱才是有意义

10维评分、命中率、覆盖率……这些数字只是中间指标，不是终点。

**真正的KPI只有一个**：今天收盘总资比昨天高了没有。

所有评分体系如果和这个KPI没有强关联，就应该简化或去掉。

## 铁律八：每天必须留下「总资」记录（真日盈亏数据链）

tools/true_daily_pnl.py 只认 TRADES.md 里带日期的「总资」记录（### YYYY-MM-DD 节 + | 总资 | $X |）。
07-10后记录断档 → 真日盈亏算不出来。2026-08-08已恢复。

**每天21:00排查cron必须执行**（幂等，当天已有则跳过）：
1. 读 data/total_asset.json 的 total（20:00反思cron已更新为真实净值）
2. 若 TRADES.md 今天无「总资」记录 → 追加：
   `### <今天> 21:20 BJT | 总权益` + `| 总资 | $X |` + `| 来源 | total_asset.json <时间> BJT |`
3. 同步到 web4 同名文件（rsync）
4. 跑 tools/true_daily_pnl.py 验证输出 TRUE/CLOSE/NOW

断档一天 → 第二天GAP诚实报0，绝不拿N天前净值冒充昨日收盘。

---

**本文件取代所有旧复盘标准中与以上铁律冲突的部分。**
